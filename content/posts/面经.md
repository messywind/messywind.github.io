---
title: "面过的面经"
date: 2023-10-30 17:40:00
password: 6693485
message: 我的多多号
tags:
- 面经
- 私密
categories:
- 杂项
code:
  maxShownLines: 11
---

## 欢乐互娱 2023.10.25 19:00 1h

1. 自我介绍。
2. 两个矩形判相交：
   1. 平行于坐标轴。
   2. 不平行于坐标轴。

可以将其中矩形的一个点判断是否在另一个矩形的内侧 → 判断点是否在向量右侧。

1. 给定一个序列，找多少个子区间和为 $Q$ → 前缀和 + map
2. 由问题 3 引出 map 与 unordered_map 区别，以及优缺点。→ 板子。
3. 给定一个可重集合 $S$，支持以下操作:
   1. 增加/删除一个数 $x$，查询平均值。 → 维护和。
   2. 在此基础上查询最大值。→ map.end()
   3. 在此基础上查中位数。→ 双指针。

一开始我说的用懒删除的堆。

1. 由问题 5.c 引出是否可以用 map 实现 → 双指针。
2. 由问题 6 引出迭代器 ++ 的过程 → 找二叉排序树后继节点。

反问：

1. 这些数据结构底层开发中要懂吗？答：会用就行。
2. 计算几何用的多吗？答：多，例如检测碰撞。

## 依图一面 2023.10.31 19:50 40min

1. 自我介绍。
2. 实现求平方根 → 浮点数二分。
   1. 二分边界 $r=n$ 的问题，应该为 $n+1$ 的理由。
3. 最长上升子序列 → $O(n \log n)$ 树状数组维护。
   1. 树状数组具体怎么维护 → 插入/查询取 max
4. 实习经历具体项目做了什么 → 问细节。

反问：

1. 公司业务是什么？答：城市级别的智能安防，涉及到图像识别，基于图像识别的结果做一些大数据处理，来给公安做一些辅助业务。
2. 什么是“算法工程化”？答：处于算法部门和工程部门中间，算法部分会不停迭代小的工程模型，把这些模型集成到开发框架里面，再把这些框架给到工程做部署，做一些性能和速度的测试，迭代框架。

## 依图二面 2023.11.9 19:30 40min

1. 连连看。
   1. 给定一个二维矩阵，且给定起点和终点，问能否经过不超过两次 $90°$ 角到达终点(路径只能经过权值为 $0$ 的点) → 模拟 + 前缀和。
2. 对徐董鑫和宋天一的评价。
   1. 舔了舔。

反问：

1. 我有什么缺点和表现不足的地方？答：代码写太长了。
2. 公司的技术栈？答：C++

## 美团一面 2024.3.27 19:00 40min

1. 自我介绍。
2. 为什么选择做前端。
3. 项目拷打。
4. 项目前端性能优化有哪些。
5. 怎么学前端的。
6. 说几个 Promise 的方法。
7. Vue2 和 Vue3 的区别。
8. 看代码说结果。

```javascript
setTimeout(() => {
    console.log(0);
}, 0);
new Promise(() => {
    console.log(1);
}).then(() => {
    console.log(2);
});
```

1. 宏队列微队列。
2. 写过组件吗？说一下写过的组件。
3. Grid 布局和 flex 布局的区别。
4. 题：

实现一个 Promise.queue 方法，支持串行执行 promise 函数并返回最终结果，要求不能用 async/await 实现。

```javascript
Promise.queue = function(promiseFunctions){
    // todo
}
Promise.queue([
    () => promise1,
    () => promise2,
    () => promise3
]).then((values)=>{
    console.log(values); // [value1, value2, value3]
}).catch((err)=>{
    console.error(err); //只要有一个promise reject，则进入 catch
});
```
